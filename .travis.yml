matrix:
  include:
    # linux
    - os: linux
      env: 
        - BUILD_TARGET=linux_cocos_new_test 
        - GEN_BINDING_AND_COCOSFILE=true
      sudo: required
      language: cpp
script:
- tools/travis-scripts/run-script.sh
before_install:
- chmod +x tools/travis-scripts/before-install.sh
- tools/travis-scripts/before-install.sh